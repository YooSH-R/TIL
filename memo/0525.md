# Django
## USER 모델 가져올 때
- models.py
  - User = settings.AUTH_USER_MODEL
  - 문자열 가져옴

- forms.py, views.py
  - User = get_user_model()
  - class 가져옴

- 역참조를 위한 user 가져올 때
  - User = request.user
  - class가 아닌 instance 가져온다