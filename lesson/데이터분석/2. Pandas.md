# import
- import pandas as pd

# 핵심 부품
- Series: 한 열
  - pd.Series([1, 2, 3])
- DataFrame: 표 전체
  - data = {'Name':['A', 'B'], 'Score':[1, 2]}
  - pd.DataFame(data)

# 데이터 불러오기
- pd.read_csv(파일경로)

# 데이터 훑어보기
- df.head(): 맨 앞 5줄
- df.head(3): 맨 앞 3줄
- df.tail(): 맨 뒤 5줄
- df.shape: 데이터 몇 행 몇 열
- df.info(): 행 개수, 열 정보, 빠진 값
- df.describe(): 평균, 최댓값 ,최솟값 등의 요약 통계

# 데이터 저장하기
- df.to_csv(파일경로, index=False): Pandsas가 자동으로 붙인 인덱스 번호 지우기

# 데이터 선택
- df['Name']: 시리즈(1차원)
- df[['Name']]: 데이터프레임(2차원)
- df[['Name', 'Age']]
- df.loc['A']: 이름으로 찾기
- df.loc['A':'C']: 주의-C까지 포함
- df.iloc['2']: 인덱스로 찾기
- df.iloc[0:3]
- df[df['Age'] >= 20]: 조건 검색, 조건 여러개일 땐 조건 ()로 감싸고비트 연산자 쓰기

# 컬럼 추가/삭제
- df['Class'] = 'A': 컬럼 추가
- df.drop('Class', axis=1): 컬럼 삭제

# 결측치(NaN, Not a Number) 탐색
- df.isna(): True/False로 답함
- df.isna().sum(): 빈 값 총 몇 개인지

# 결측치 처리
- df.dropna(): NaN 하나라도 있는 행 삭제
- df.fillna(0): NaN 모두 0으로 바꿈
- df.fillna(method='ffil'): 바로 앞의 값으로 빈칸 채움

# 중복 제거
- df.duplicated(): 중복 여부 확인
- df.drop_duplicates(): 중복 제거

# 데이터 타입 변환
- df.info()로 확인 후 object(문자열)이라고 나오면 변환필요
  - df['Age'].astype('int')
- pd.to_numeric(df, errors='coerce'): 데이터를 숫자로 변환, 변환 못하는 값을 NaN으로 바꿈

# 문자열 다루기
- 문자열 함수 컬럼 전체에 적용하고 싶다면 .str 붙여야함
- df['Date'].str[0:4]: 연도만 자르기
- df['Name'].str.strip(): 앞뒤 공백 삭제
- df['Name'].str.upper(): 대문자로
- df[df['Name'].str.contains('Seoul')]: 포함된 단어 선택
- df['Price'].str.replace(',', '').str.replace('원',''): ','와 '원'삭제

# 날짜 다루기
- df['Date'] = pd.to_datetime(df['Date']): 날짜 타입으로 바꾸기
- df['Date'].dt.year
- df['Date'].dt.month
- df['Date'].dt.day
- df['Date'].dt.day_name()

# 데이터 정렬
- df.sort_values(by='Score'): 오름차순 정렬
- df.sort_values(by='Score', acending=False): 내림차순 정렬
- df.sort_values(by='Score', ascending=False).head(3): 상위3명
- df.sort_values(by=['group', 'score'], ascending=[True, False]): 여러 기준

# 기초 통계량
- df['Score'].mean()
- df['Score'].median()
- df['Score'].max()
- df['Score'].min()
- df['Score'].sum()
- df['Product'].value_counts(): 어떤 종류가 몇 개나 있나
- df.describe(): 여러 통계량 한번에(숫자형 데이터만)
- df.describe(inclue='0'): 문자열 데이터도 보려면

# 그룹 연산
- df.groupby('Class')['Math'].mean(): Class별로 묶어서 Math점수의 평균 구하기
- df.groupby('Dept')['Salary'].agg(['mean', 'max']): 원하는 통계량들 리스트에 담아서 한번에 주문
- df.groupby('Dept').agg({'Salary': 'mean', 'Bonus': 'sum'}): 딕셔너리 사용해서 더 정교하게 주문
- df.groupby('team')['score'].transform('mean'): 그룹별로 계산한 값을 원래 인덱스에 맞춰서 돌려줌

# 피벗 테이블
- df.pivot_table(index='Year', columns='Team', values='Sales', aggfunc='sum')

# 멜트
- df.melt(id_vars='Name', value_vars=['2023', '2024]): id_vars - 가만히 놔둘 컬럼(기준), value_vars - 녹여버릴 컬럼들
- df.melt(id_vars='City', var_name='Day', value_name='Temp'): 녹여버릴 컬럼과 그 값 이름 지정

# 이어 붙이기
- pd.concat([df1, df2]): 인덱스 중복 발생
- pd.concat([df1, df2], ignore_index=True): 인덱스 번호 다시 매김
- pd.concat([df1, df2], axis=1): 좌우로 나란히 붙임

# 병합하기
- on: 기준 되는 열 이름(양쪽 표에 다 있어야 함)
- how: 합치는 방식
  - inner: 교집합
  - outer
  - left: 왼쪽 표는 다 살리고, 오른쪽 표에서 정보 찾기, 없으면 NaN
  - right
- pd.merge(df1, df2, on='ID', how='inner')
- pd.merge(df1, df2, on='ID', how='left')
- pd.merge(df1, df2, left_on='ID', right_on='User_ID'): 컬럼 이름이 다를 때
- pd.merge(df1, df2, on='Code', suffixes=('_Left', '_Right')): 이름 충돌 해결
- pd.merge(df1, db2, on='key', how='outer', indicator=True): 각 행이 어느 데이터에서 왔는지 _merge 컬럼에서 표시