# 선형회귀
## 선형회귀(Linear Regression)
- 입력 변수와 출력 변수 사이의 관계를 직선 형태로 근사하여, 예측하는 통계적 방법

# 단순선형회귀(Simple Linear Regression)
## 단순선형회귀: 단일 변수를 이용한 선형회귀
- 한 개의 설명변수(X)와 하나의 반응변수(Y) 사이의 선형(직선) 관계를 찾는 방법
 
## 최소제곱법(least squares)
- 실제 관측값과 예측값의 차이(잔차, residual)를 제곱해 합한 값(RSS, 잔차제곱합)을 최소화하는 방법
- 목표: 데이터를 가장 잘 설명하는 직선을 찾기 위해 계수 B0(절편), B1(기울기)을 추정

# 다중선형회귀(mulstiple linear regression)
- 독립 변수(설명 변수, Feature)가 여러 개 존재할 때 사용하는 회귀 분석 기법
- 여러 독립 변수(X1, X2, ...)를 동시에 고려하여, 종속 변수(Y)와의 관계를 구함

## 다중선형회귀의 추정과 예측
- 추정 방법: 실제 값과 예측 값의 차이(잔차, ei = yi - ^yi)를 제곱해 합한 값(RSS)를 최소화

# 분류
## 분류 문제에서 적합한 모델
- 분류 문제에서 선형회귀의 대안 - 로지스틱 회귀(Logistic Regression)
  - 시그모이드(Sigmoid) 함수를 활용해 0~1 범위 내 확률값 예측 보장
  - 순서가 없는 범주를 확률로 직접 예측하는 적절한 분류 방법

## 로지스틱회귀
- 오즈(Odds)
  - 성공(y = 1)확률이 실패(y = 0)확률에 비해 몇배 더 높은가를 나타낸다
- 로짓 변환(logit) = Log odds
  - 로짓변환은 오즈(odds)에 log를 취한 함수 형태
- 로지스틱 모형식은 선형 모형식과 시그모이드 함수의 결합이며, 로짓 변환시 선형회귀 모형식으로 표현이 가능하다.

## MLE활용 모수 추정
- 우도(Likelihood)
  - 모수(B0, B1)를 추정하는 방법
  - 현재 확률 함수가 데이터를 얼마나 잘 설명하는지를 나타낸 지표
  - 모델의 학습은 우도 값을 높여 최대화가 되도록 하는 것이 목표이며 이를 Maximum Likelihood Estimation(MLE)이라 한다.

- 로지스틱 회귀의 우도 최대화
  - 양 변에 log를 취해 곱셈을 더하기로 변환시킨 뒤 log-likelihood를 만들어 최대화한다.

# 신경망모델(Neural Network)
## Shallow 네트워크
- 입력층(Input) - 은닉층(Hidden Layer) 1개 - 출력층(Output)으로 구성된 1개의 은닉층만 가진 신경망 구조
- Hidden Layer는 각자의 activation 함수를 가진 hidden unit으로 구성

## Deep 네트워크

# 신경망 적합(Fitting)
## 선형회귀 예시
### 손실함수
- 학습 데이터셋: input/output의 I개 쌍
- 손실함수(Loss function): 모델이 얼마나 잘못 예측하는지를 측정하는 함수

### 1D 선형회귀 학습
- 경사 하강법(gradient descent): 손실 함수의 값이 줄어드는 방향으로 파라미터를 이동하는 과정

## 경사 하강
### 함수에 따른 최적화 난이도
- Convex vs Non-convex 최적화 문제
  - Convex: 곡선이 항상 U자처럼 아래로 볼록. 그래프 위 임의 두 점을 잇는 직선이 그래프 위(또는 같은위치)로 있음.
  - Non-convex: 봉우리&middot;골짜기&middot;오목한 구간이 섞인 모양. 두 점을 이은 직선이 그래프 아래로 내려가는 구간이 생김.
  - 손실 함수 모양에 따라 최적화의 난이도가 달라짐
    - Convex 문제: 전역(global) 최소값이 유일함 -> 최적화가 쉬움
    - Non-Convex 문제: 여러 개의 지역(local) 최소값 또는 새들(saddle)점이 있음 -> 최적화가 어려움

## 확률적 경사 하강법(SGD)
### 경사 하강법 vs 확률적 경사 하강법
- 경사 하강법의 단점
  - Non-convex 문제에서 지역 최소점에 빠지기 쉬움
  - 매 스텝마다 전체 데이터에 대한 미분값을 구하여 업데이트 함 -> 스텝별 계산량이 많음

- 대안: 전체 데이터를 한번에 쓰는 대신, 무작위로 선택한 데이터 샘플 사용
  - 확률적 경사 하강법(Stochastic Gradient Descent, SGD)

### 확률적 경사 하강법의 특성
- 국소 최솟값
  - 전체 데이터가 아닌 일부 배치로 기울기를 계산하기 때문에 노이즈가 섞여 있음
  - 노이즈가 오히려 local minima, saddle point에서 빠져나오는 도움이 됨
- 노이즈가 있지만 여전히 타당한 업데이트
  - 학습이 점진적 최적점 방향으로 수렴
- 계산 비용 절감
- 수렴 특성
  - full batch처럼 매끄럽게 수렴하지 않고, 무작위성 때문에 더 많이 진동하면서 움직임.
  - 전역 최솟값 근처의 좋은 해에 도달할 수 있음. Convex 문제에서는 full-batch 경사하강보다 수렴이 늦을 수 있음

## 역전파(Backpropagation)
### 네트워크 파라미터의 미분
- 연쇄법칙

### 역전파
- 출력 오차를 기준으로 그래프를 거꾸로 따라가며 연쇄법칙으로 각 노드(파라미터 포함)의 미분값을 계산하는 절차